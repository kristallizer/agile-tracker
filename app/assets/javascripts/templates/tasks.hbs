<table class="table" id="task-list">
  <tbody>
    {{#each tasks itemController='task'}}
      <tr>
        <td>
          {{input type=checkbox checked=completed}}
          <label {{bind-attr class="completed:completed"}}>{{name}}</label>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
